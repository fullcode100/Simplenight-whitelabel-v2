import { Item } from 'types/booking/bookingType';

interface HotelConfirmationBodyProps {
  item?: Item;
}

const HotelConfirmationBody = ({ item }: HotelConfirmationBodyProps) => {
  return <>Body</>;
};

export default HotelConfirmationBody;
