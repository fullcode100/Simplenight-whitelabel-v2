import { Item } from 'types/booking/bookingType';
import { CategoryOption } from 'types/search/SearchTypeOptions';

interface TransportationCancelledDisplayProps {
  item?: Item;
  Category: CategoryOption;
}

const TransportationCancelledDisplay = ({
  item,
  Category,
}: TransportationCancelledDisplayProps) => {
  return <></>;
};

export default TransportationCancelledDisplay;
