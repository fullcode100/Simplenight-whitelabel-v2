### Standard terms component

##### Terms default

```jsx padded
const [check, setCheck] = useState(false);


<Terms checkValue={terms} checkboxMethod={setTerms} />
```

##### Terms disabled

```jsx padded
const [check, setCheck] = useState(false);

<Terms checkValue={terms} checkboxMethod={setTerms} disabled={true} />
```
